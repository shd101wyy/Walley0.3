;; Walley Language test file
(display-string "Hello World\n")
(def test2 
     (lambda []
       (def x 12)
       (lambda [msg]
	 (display-string msg)
	 (if (eq? msg 'x)
	     x
	   (set! x 15)))))
(def x (test2))
x
(eq? 'x 'x)

