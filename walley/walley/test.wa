;; Walley Language test file
;;(display-string "Hello World\n")
(def test2 
     (lambda []
       (def x 12)
       (lambda [msg]
	 (if (eq? msg 'x)
	     x
	   (set! x 13)))))
(def x (test2))
(x 'x)
(x 'a)
(x 'x)
